
var imports = require("./apple");

imports.save(3); //saves a number to a list

imports.save(7); //saves a number to a list


imports.save(1); //saves a number to a list

imports.save(17);

imports.save(8);

console.log(imports.sorted()); //sorts the list in ascending order


